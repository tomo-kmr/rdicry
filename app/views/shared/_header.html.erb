<div class='header' >
  <div class='header-title'>
    <%=link_to 'プログラミング初心者のためのまとめ', root_path %>
  </div>
  <p class='header-sub-title'>
    Ruby on railsを学習し始めた自分が、〜したい時どう書くんだったっけ？をまとめたサイトです
  </p>
  <%= form_with(url: search_codes_path, local: true, method: :get, class: "search-form") do |form| %>
      <%= form.text_field :keyword, placeholder: 'キーワードから探す', class: 'input-box' %>
      <button class="search-button">
        検索
      </button>
  <% end %>
  <div class='first-tab'>
    <div class='first-note'>
      <%=link_to 'はじめに', users_path, class:'first-note-link' %>
    </div>
    <div class='sign-in-out'>
      <% if user_signed_in? %>
        <%=link_to 'ログアウト', destroy_user_session_path, method: :delete %>
      <% else %>
         <%=link_to 'ログイン', new_user_session_path %>
      <% end %>
    </div>
    <% if user_signed_in? && current_user.admin? %>
      <div class='make-new-code'>
        <%=link_to '新規作成', new_code_path %>
      </div>
    <% end %>
  </div>
 </div>